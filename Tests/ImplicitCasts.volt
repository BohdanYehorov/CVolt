fun:i32 Main()
{
    let:i32 a = 45i8;
    let:i8  b = 85;
    let:f32 c = 5.4;
    let:f32 d = a;
    let:f64 e = 58.4f32;
    let:i32 f = a + e;

    Out(a);
    Out(b);
    Out(c);
    Out(d);
    Out(e);
    Out(f);

    return 0;
}